# TODO [RENAME-TRACKER]: Update all repository URLs and metadata references from 'FloStyle/m7-jellyfin' to 'FloStyle/m7-jellyfin-neo' before executing the repo rename.
name: Dev Build

on:
  push:
    branches:
      - main
  schedule:
    # Every day at 02:00 UTC
    - cron: '0 2 * * *'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Prepare source directory
        run: cp -r . /tmp/m7-jellyfin-src
      - name: Build source tarball
        run: |
          DATE=$(date -u +%Y%m%d)
          cd /tmp/m7-jellyfin-src
          tar czf "${GITHUB_WORKSPACE}/m7-jellyfin-dev-${DATE}.tar.gz" \
            --exclude='dist' \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='scripts/.ps3-test.env' \
            .
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install pnpm
        run: npm install -g pnpm
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Build plugin
        run: pnpm run build
      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: dev
          name: Dev build
          body: |
            Auto-built from `main` — **not for production use**.

            Build date: ${{ github.event.head_commit.timestamp || 'scheduled' }}
            Commit: [${{ github.sha }}](https://github.com/FloStyle/m7-jellyfin/commit/${{ github.sha }}) <!-- RENAME-TRACKER: Update to m7-jellyfin-neo -->
          prerelease: true
          make_latest: false
          files: |
            dist/jellyfin.zip
            m7-jellyfin-dev-*.tar.gz
